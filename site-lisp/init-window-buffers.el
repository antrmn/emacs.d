(setopt temp-buffer-max-height 15
        temp-buffer-resize-mode t)

(setopt switch-to-buffer-in-dedicated-window 'pop
        switch-to-buffer-obey-display-actions t)
(setopt ibuffer-old-time 24)

(provide 'init-window-buffers)
